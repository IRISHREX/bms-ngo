{
  "name": "ssh-deploy",
  "version": "1.0.0",
  "description": "Universal SSH deployment tool for any project",
  "scripts": {
    "deploy": "node deploy.js",
    "fetch": "node fetch.js"
  },
  "dependencies": {
    "node-ssh": "^13.2.0"
  }
}
